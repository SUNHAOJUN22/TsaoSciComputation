# Qualification scope

The candidate tree is accepted only after two full repository verification passes plus native C ABI, CTest, and Python bridge verification. The workflow commits directly to `main`, removes all transport files, and creates no other branch.
