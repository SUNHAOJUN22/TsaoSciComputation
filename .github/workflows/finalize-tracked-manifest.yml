name: Finalize tracked manifest (closure-disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Superseded by sync-closure-evidence during final closure.'
