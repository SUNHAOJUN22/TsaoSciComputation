name: Export current main V4 once

on:
  issues:
    types: [opened]

permissions:
  actions: read
  contents: write
  issues: write

concurrency:
  group: export-current-main-v4-once
  cancel-in-progress: false

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  export:
    if: >-
      github.event.issue.title == 'EXPORT_CURRENT_MAIN_V4_20260805' &&
      github.event.issue.user.login == github.repository_owner
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: true

      - name: Build exact source snapshot
        shell: bash
        run: |
          set -Eeuo pipefail
          python - <<'PY'
          from pathlib import Path
          import tarfile

          root = Path.cwd()
          output = root / 'TsaoSciComputation-main-v4.tar.gz'
          excluded = {'.git', '.venv', '__pycache__', '.pytest_cache', '.mypy_cache', '.ruff_cache', 'dist', 'build'}
          with tarfile.open(output, 'w:gz', format=tarfile.PAX_FORMAT) as archive:
              for path in sorted(root.rglob('*')):
                  relative = path.relative_to(root)
                  if any(part in excluded for part in relative.parts):
                      continue
                  if path == output or path.is_dir():
                      continue
                  archive.add(path, arcname=relative.as_posix(), recursive=False)
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: TsaoSciComputation-main-v4-${{ github.run_id }}
          path: TsaoSciComputation-main-v4.tar.gz
          if-no-files-found: error
          retention-days: 3

      - name: Report artifact
        shell: bash
        run: |
          gh issue comment "${{ github.event.issue.number }}" --repo "$GITHUB_REPOSITORY" --body "Current main V4 source exported. run_id=$GITHUB_RUN_ID commit=$(git rev-parse HEAD)"

      - name: Remove one-time workflow
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/export-current-main-v4-once.yml
          git commit -m 'Remove one-time V4 source export workflow [skip ci]'
          git push origin HEAD:refs/heads/main

      - name: Close export issue
        shell: bash
        run: |
          gh issue close "${{ github.event.issue.number }}" --repo "$GITHUB_REPOSITORY" --reason completed --comment 'Current main source exported without creating a branch or pull request.'
