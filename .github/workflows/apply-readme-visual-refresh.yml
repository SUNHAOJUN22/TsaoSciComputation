name: Apply README visual refresh

on:
  push:
    branches:
      - agent/refresh-readme-visuals-uiux-pro-max
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  regenerate:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging verified payload fragments"
