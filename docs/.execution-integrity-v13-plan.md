# Execution Integrity V13 temporary plan

This temporary tracked plan exists only to anchor the fail-closed validation workflow. It must be removed by the final certified commit.

Scope:
- remove public boolean process-execution bypass;
- separate read-only probes from authorized computation execution;
- bind authorization to current executable and input file contents;
- unify Python-module probe environment;
- add cross-platform regression and fault-injection tests.
